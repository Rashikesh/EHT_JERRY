import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // swcMinify is true by default in Next.js 14+, so we don't need it here
  reactStrictMode : false,
};

export default nextConfig;
